name: Publish Python package to PyPI

on:
  push:
    tags:
      - "v*"
      - "py-aikernel-wasm-*"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    name: Build aikernel-wasm
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: python
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install build tooling
        run: python -m pip install --upgrade build twine

      - name: Build distributions
        run: python -m build --sdist --wheel --outdir dist

      - name: Check distributions
        run: python -m twine check dist/*

      - name: Upload distributions
        uses: actions/upload-artifact@v4
        with:
          name: pypi-dist-aikernel-wasm
          path: python/dist/*
          if-no-files-found: error

  publish:
    name: Publish aikernel-wasm
    runs-on: ubuntu-latest
    needs: build
    environment:
      name: pypi
    permissions:
      contents: read
      id-token: write
    steps:
      - name: Download distributions
        uses: actions/download-artifact@v4
        with:
          name: pypi-dist-aikernel-wasm
          path: dist

      - name: Show trusted publisher claims
        run: |
          echo "PyPI project: aikernel-wasm"
          echo "Expected owner: AIKernel-NET"
          echo "Expected repository: AIKernel.Wasm"
          echo "Expected workflow: publish-pypi.yml"
          echo "Expected environment: pypi"
          echo "Actual repository: ${GITHUB_REPOSITORY}"
          echo "Actual workflow ref: ${GITHUB_WORKFLOW_REF}"
          echo "Actual ref: ${GITHUB_REF}"
      - name: Publish distributions to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
